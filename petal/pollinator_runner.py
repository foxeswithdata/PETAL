

class PollinatorRunner:

    def __init__(self, colony_pollinator_traits, solitary_pollinator_traits):
        self.pollinators = []

        self.initColonyPollinators(colony_pollinator_traits)
        self.initSolitaryPollinators(solitary_pollinator_traits)

    def initColonyPollinators(self, colony_pollinator_traits):
        # read file
        print("to do")

    def initSolitaryPollinators(self, solitary_pollinator_traits):
        # read file
        print("to do")

